{
  "name": "journey-noir-travelapp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check api/aggregate.js"
  },
  "dependencies": {
    "firebase": "^12.15.0"
  }
}
